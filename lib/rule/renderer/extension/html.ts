import { render_latex } from "../../../common/render_latex"
import { EXBRendererRuleProcessor } from "../extension_block"

const html: EXBRendererRuleProcessor = async (header, content, env) => {
  // if (header === "html") {
  //   return content
  // }
  // return false
}

export default html